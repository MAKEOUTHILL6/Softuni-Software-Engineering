function reading(bookPages, pagePerHour, days){
    let totalReadingHours = bookPages / pagePerHour;
    let requiredHours = totalReadingHours / days;
    console.log(requiredHours)
}
reading(212,
    20 ,
    2 
    )